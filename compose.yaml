name: ip-info

services:
  ipinfo:
    image: ediwang.azurecr.io/ipinfo:latest
    container_name: ipinfo
    restart: unless-stopped
    ports:
      - "8000:8080"
    environment:
      ASPNETCORE_URLS: http://+:8080
      DBPath: /data/qqwry.dat
      IpDatabases__Providers__0__Type: Qqwry
      IpDatabases__Providers__0__Path: /data/qqwry.dat
      IpDatabases__Providers__1__Type: MaxMindGeoLite2City
      IpDatabases__Providers__1__Path: /data/GeoLite2-City.mmdb
      IpDatabases__Providers__1__Locale: zh-CN
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:8080/health/ready >/dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    volumes:
      - type: bind
        source: /opt/docker/ip-info
        target: /data
        read_only: true

  ipinfo-updater:
    image: ediwang.azurecr.io/ipinfo-updater:latest
    container_name: ipinfo-updater
    restart: unless-stopped
    environment:
      DATA_DIR: /data
      DATABASES: qqwry,geolite2-city
      MAXMIND_LICENSE_KEY: ${MAXMIND_LICENSE_KEY:-}
      SCHEDULE_TIME: "03:00"
    volumes:
      - type: bind
        source: /opt/docker/ip-info
        target: /data
      # Follow the VM's local timezone so "03:00" means 03:00 on the host.
      - type: bind
        source: /etc/localtime
        target: /etc/localtime
        read_only: true
    entrypoint:
      - /bin/bash
      - -lc
    command:
      - |
        set -euo pipefail

        log() {
          echo "[$$(date -Is)] $$*"
        }

        run_update() {
          log "Running IP database update..."
          if /app/update.sh; then
            log "IP database update finished."
          else
            rc="$$?"
            log "IP database update failed with exit code $${rc}."
            return "$${rc}"
          fi
        }

        log "Running initial database update."
        run_update || true

        while true; do
          now="$$(date +%s)"
          target_today="$$(date -d "$$(date +%F) $${SCHEDULE_TIME}:00" +%s)"

          if [ "$${target_today}" -le "$${now}" ]; then
            next_run="$$(date -d "tomorrow $${SCHEDULE_TIME}:00" +%s)"
          else
            next_run="$${target_today}"
          fi

          sleep_seconds="$$((next_run - now))"
          log "Next update at $$(date -d "@$${next_run}" -Is); sleeping $${sleep_seconds}s."
          sleep "$${sleep_seconds}"
          run_update || true
        done
